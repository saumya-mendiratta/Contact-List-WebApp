<!DOCTYPE html>
<html lang="en">
<head>
   
    <title>
        <%= title %>
         <!-- OR  -->
        <!-- <%= locals.title %>  -->
    </title>
</head>
<body>
    <h1>Playground</h1>
    
    <ul>
        <% for(let i=1;i<6;i++) { %>

           <li> 
                <%= i%>
            </li>

         <% } %>
    </ul>

    <% if(true) { %>
        <h1>It's True</h1>
    <% }else{ %>
        <p>It's False</p>
    <% } %>

</body>
</html>